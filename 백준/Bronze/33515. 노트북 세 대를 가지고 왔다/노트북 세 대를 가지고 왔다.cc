#include <bits/stdc++.h>
#define MOD 1'000'000'007
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int t1,t2;
    cin>>t1>>t2;

    if(t1>=t2) cout<<t2;
    else {
        cout<<t1;
    }
}