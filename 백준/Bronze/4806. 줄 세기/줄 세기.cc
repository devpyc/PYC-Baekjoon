#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string s;
    int cnt=0;
    while(getline(cin,s)) {
        cnt++;
    }
    cout<<cnt<<"\n";
}